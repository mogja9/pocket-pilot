{
  "name": "pocket-pilot",
  "version": "0.0.1",
  "private": true,
  "description": "A move-optimizer engine for Pokemon TCG Pocket: given a live board state, rank the legal plays by win-equity under the game's randomness (coin flips, energy generation, hidden info).",
  "type": "module",
  "scripts": {
    "demo": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/engine.test.ts && tsx src/scenarios.test.ts && tsx web/dom.test.ts && tsx web/app.smoke.ts",
    "web:dev": "vite",
    "web:build": "vite build",
    "build:data": "node scripts/scrape-limitless.mjs"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "@types/node": "^22.0.0",
    "jsdom": "^29.1.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
